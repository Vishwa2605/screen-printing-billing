(function supabaseNotes() {
  /*
    This file is a placeholder for Phase 2.

    Planned responsibilities:
    - create Supabase client from window.SUPABASE_CONFIG
    - fetch shared products, messers, invoices, invoice items, and production records
    - replace LocalStorage operations gradually
    - subscribe to realtime changes for cross-device updates

    This is intentionally not active yet because the real connection
    needs a Supabase project URL and anon key.
  */
})();
